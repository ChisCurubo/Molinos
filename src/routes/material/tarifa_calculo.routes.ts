import { Router } from 'express';
import { factory } from '../../config/factory';

const router = Router();

// TODO: Restaurar rutas de tarifa calculo
// router.get('/', controller.listar);

export default router;
