import { Router } from 'express';
import { factory } from '../../config/factory';

const router = Router();

// TODO: Restaurar rutas de proveedor
// router.get('/', controller.listar);

export default router;
